Ruinning this PROJECT:

Unzip the PROJECT.zip file

Build the project by typing:

	ant build jar

This will compile the project and build a jar package.

Run the packaged by typing:

	java -jar AnswerMyQuestion.jar <input file>
